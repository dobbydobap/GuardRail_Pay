{
  "name": "agentvault-backend",
  "version": "0.1.0",
  "description": "GuardRail Pay / AgentVault — on-chain policy firewall for AI agent payments (backend MVP, mocked)",
  "type": "module",
  "scripts": {
    "dev": "bun --watch src/index.ts",
    "start": "bun src/index.ts"
  },
  "dependencies": {
    "bun-types": "^1.3.14",
    "ethers": "^6.16.0",
    "openai": "^6.42.0"
  },
  "devDependencies": {
    "@types/bun": "^1.3.14",
    "typescript": "^5.9.3"
  }
}
